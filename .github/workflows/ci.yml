name: CI/CD Pipeline

on:
  push:
    branches:
      - development
      - master
  pull_request:
    branches:
      - development
      - master

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:5.7
        env:
          MYSQL_ROOT_PASSWORD: root
        options: --health-cmd "mysqladmin ping --silent" --health-interval 10s --health-timeout 5s --health-retries 3

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2.12'
          extensions: mbstring, intl, pdo_mysql

      - name: Install dependencies
        run: composer install 

      - name: Run tests
        env:
          DATABASE_URL: "DATABASE_URL="mysql://sql7740449:8KpMy7sQSq@sql7.freemysqlhosting.net:3306/sql7740449?serverVersion=5.5.62-MariaDB&charset=utf8mb4"
        run: php bin/phpunit
